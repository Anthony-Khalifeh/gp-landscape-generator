#### Code for generating landscapes 


it just uses numpy and matplotlib so u don't really need an env but if u do its there

activate it with
```
conda env create -f environment-2.yml
conda activate gp-landscape-generator
```
